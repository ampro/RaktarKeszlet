@model IEnumerable<RaktarKeszlet.Models.StorageContainer>

<div class="table-responsive shadow-sm rounded border bg-white mb-3">
    <table class="table table-hover align-middle mb-0">
        <thead class="table-dark">
            <tr>
                <th>Tárolóeszköz megnevezése</th>
                <th>Típus</th>
                <th class="text-end">Műveletek</th>
            </tr>
        </thead>
        <tbody>
            @if (!Model.Any())
            {
                <tr><td colspan="3" class="text-center py-3 text-muted">Nincsenek tárolóeszközök ezen a polcon.</td></tr>
            }
            @foreach (var item in Model)
            {
                <!-- Fúrás a Tárolóeszköz szintjére -->
                <tr style="cursor: pointer;" onclick="window.location.href='/StorageContainers/Details/@item.Id'">
                    <td class="fw-bold text-primary"><i class="bi bi-box"></i> @item.Name</td>
                    <td>@item.Type</td>
                    <td class="text-end">
                        <a asp-controller="StorageContainers" asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">Megnyitás</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>